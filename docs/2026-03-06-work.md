# 📅 2026-03-06 Development Log

## Nav UX 개선 및 DetailPage 최적화

### 1. Nav 로그인 상태 UX 개선

- 로그인 상태에서 `/` 랜딩 페이지 접근 시 로그인 버튼 대신 사용자 프로필 UI 표시
- 로그인 상태에서 `/` 페이지에 **앱으로 가기 버튼** 추가
- `/main`과 `/` Nav UI 동작 분리
- `/` 페이지에서는 영화 검색 인풋을 표시하지 않도록 수정

**결과**

- 로그인 상태 UX 혼동 문제 해결
- `/` 페이지에서도 사용자 상태가 명확하게 표시됨
- `/main`과 `/` Nav 역할 분리로 UI 구조 개선

---

### 2. DetailPage Hero 이미지 로딩 안정화

- Hero 이미지 렌더링 시 preload 처리 추가
- 이미지 로드 완료 후 skeleton UI 제거하도록 수정
- 이미지 로드 실패 시 fallback 처리

**결과**

- Hero 이미지 로딩 시 깜빡임 감소
- Layout shift 방지
- DetailPage 초기 렌더 안정화

---

### 3. DetailPage Scroll 인터랙션 개선

- 스크롤 위치 기반 Hero vignette 효과 적용
- 스크롤 시 Hero 영역이 자연스럽게 어두워지도록 수정

**결과**

- 콘텐츠 가독성 개선
- OTT 서비스 스타일 Hero 인터랙션 구현

---

### 4. Trailer iframe 성능 및 접근성 개선

- Trailer iframe에 lazy loading 적용
- iframe title 속성 수정

**결과**

- 초기 렌더 성능 개선
- ESLint 접근성 경고 해결

---

### 5. Cast 리스트 렌더링 최적화 및 레이아웃 개선

- Cast 리스트에 useMemo 적용
- 불필요한 re-render 방지
- 데스크탑과 모바일 Cast 리스트 분리

**결과**

- DetailPage 렌더 성능 개선
- 컴포넌트 업데이트 안정화
- 데스크탑 모드에서만 Cast 리스트 표출
- 모바일 모드에서는 Cast 리스트 제거로 영화 정보 가독성 용이

---

### 6. DetailPage 데이터 요청 구조 개선

- DetailPage 데이터 fetch와 Hero 이미지 preload 로직 분리
- useEffect 의존성 구조 정리

**결과**

- 불필요한 API 재요청 방지
- DetailPage 데이터 로딩 구조 안정화

---

### 7. ESLint 및 코드 품질 정리

- 사용하지 않는 변수 제거
- React Hook dependency 경고 해결
- iframe 접근성 경고 해결

**결과**

- ESLint 경고 제거
- 코드 안정성 및 가독성 개선

---

### 8. Row 컴포넌트 구조 및 UX 개선

- Row 컴포넌트 데이터 요청 구조 정리
- 동일 query 요청 시 재요청을 방지하기 위해 캐시 구조(`ROW_CACHE`) 적용
- Skeleton UI가 스크롤 시 반복적으로 나타나던 문제 수정
- 카드 Hover 시 영화 제목을 표시하는 Overlay UI 추가
- Row 좌우 여백 및 카드 간격 조정
- 카드 클릭 시 DetailPage로 이동하는 navigation 동작 안정화

**결과**

- Row 데이터 요청 안정성 개선
- 불필요한 API 요청 감소
- Skeleton UI 반복 로딩 문제 해결
- 콘텐츠 식별 UX 개선
- DetailPage 및 MainPage 콘텐츠 탐색 경험 향상


## Summary

- Nav 로그인 상태 UX 개선
- 랜딩 페이지와 메인 페이지 Nav 구조 분리
- DetailPage Hero 이미지 로딩 안정화
- Scroll 기반 Hero 인터랙션 개선
- Trailer iframe 성능 및 접근성 개선
- Cast 리스트 렌더링 최적화
- DetailPage 데이터 요청 구조 정리
- ESLint 경고 및 코드 품질 개선
- Row를 단순 리스트 컴포넌트 → 콘텐츠 플랫폼 핵심 컴포넌트로 안정화``