# 2026-03-10 작업 기록 (최신화)

업데이트 기준일: 2026-03-11

## 1) 스크롤 복원 로직
- `ScrollManager` 중심으로 라우트/새로고침/뒤로가기 정책 보강
- `/main`의 검색/피드백/상세 왕복 시 스크롤 복원 누락 케이스 수정
- 모달->상세 이동 시 모달 언마운트 복원 간섭 방지 처리

## 2) 상태 UI 분기 체계 확장
- Row / Top10 / Hero / Search / Detail에 상태 분기 일관 적용
- 성공 상태 외 배지 표시 규칙 정리
- fallback UI(이미지 실패, empty, error) 가독성/일관성 개선

## 3) Hero 개선
- CDN 실패/로딩에서 최소 배경 유지
- 자동 슬라이드와 사용자 조작 충돌 완화
- 모바일/초소형 반응형 보강
- 상태 박스 위치를 nav 기준 상단 계산으로 조정

## 4) Search 개선
- `loading` 상태에서 중앙 스피너/문구 표기
- `no-image/cdn-fail` 텍스트 깨짐 제거(내부 라벨 렌더로 전환)
- fallback 이미지의 외부 텍스트/규격 숫자 표기 제거

## 5) Banner/Detail 디버그 개선
- Banner `success`는 더미 데이터 제거, 실제 API 기준 렌더
- `bannerDebug=image-error` URL 직접 테스트 가능하도록 허용
- Detail은 `detailDebugState`를 URL 파라미터로도 테스트 가능하게 확장

## 6) Top10 / Modal 반응형 강화
- Top10 상태 UI의 모바일/초소형 폰트/박스/여백 조정
- 상태 모드에서 좌우 이격(성공/기본 렌더 제외) 적용
- MovieModal 긴 텍스트 clamp 처리로 내부 스크롤 유발 방지
- 모바일 우선으로 모달 높이/패딩/폰트/버튼 크기 재조정

## 7) 테스트/문서
- `scroll-restoration.spec.js` 보강 및 반복 실행
- `debug-url-matrix` 최신화
- 수동 테스트 문서(`scroll-restoration-manual-test.md`) 업데이트

## 8) 남은 이슈
- Row 딥 스와이프 복원: 기대 인덱스 대비 복원 인덱스 불일치
- 후속으로 스와이프 복원 기준(translate/progress) 정합화 필요
